void sortZeroesAndOne(int *input, int size){
    int counter=0;
    for(int i=0;i<size;i++){
        if(input[i]==0){
            int temp=input[i];
            input[i]=input[counter];
            input[counter]=temp;
            counter++;
        }
    }
    
}
